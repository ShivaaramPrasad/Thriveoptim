package dsa.recursion;

import java.util.LinkedList;

import org.junit.Test;

public class Introduction {
	/*
 The Function calls itself is called Recursion
 void fun()   //This function 
 {
 fun();  //is calling itself
 }
 // Basic rules of Recursion
  1. Function Should Call Itself 
  void fun(N)
  {
   fun(N-1);  
  }
  2. Function should have the base case 
  void fun(N)
  {
    if(N==1)  
       return;
  fun(N-1); // 3. Recursive Call should Align towards the base class 
  }

  4. if We have N-1 is changed to N+1 it will be a endless loop and it not reach the base case
	 
	@Test
	public void test() {
		int n=5;
		System.out.println(recFib(n));
	}

	private int recFib(int n) {
		if( n == 1) return 1;
		return recFib(n-1);
	}
*/

}
