package algo;

public class BoyerMooreVoting {
	/*
	Boyer–Moore majority vote algorithm has two steps: 
      if count equal to 0   assign nums to majority 
      if majority is equal to nums increment the count 
      else count--
      return majority element
      
        int count = 1;
        int majele = 0;
        for(int i=0;i<nums.length;i++){
            if(count == 0) element = nums[i];
            if(majele == nums[i]) count++;
            else count--;
        }
        return majele;
   */
}