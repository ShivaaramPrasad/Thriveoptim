package algo;

public class KadaneAlgorithm {
	
	/*
	 1. Algorithm to solve maximum sum sub array
	 2. Complexity
	   a.Time Complexity-O[N]
	   b.Space Complexity-O[1]
	  3. Condition- if the sum is less than or zero, rest Window sum as 0
	  		
	  			 
	 maxSum=Math.max(sum, maxSum);
	 
	 Example : MaximumSubarray_53

	 122
*/
}
